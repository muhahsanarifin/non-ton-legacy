export type contentProps = {
  [key: string]: any;
};

export type formProps = {
  onSetQuery: any;
  onSetPages: any;
};

export type paginationProps = {
  onTotalPages: number;
  onSetPages: any;
  onPage: number;
};

export type ModalProps = {
  onIdModal: string;
};
