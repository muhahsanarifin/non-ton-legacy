export type contentProps = {
  [key: string]: any;
};

export type formProps = {
  onSetQuery: any;
};

export type paginationProps = {
  onTotalPages: number;
  onSetPages: any;
  onPage: number;
};
